# RAG 知识层 Python 依赖
# 安装: pip install -r rag/requirements.txt
#
# 注意: numpy 和 faiss 编译依赖重，单独列在 optional 组。
#       如果 pip install 报编译错误，先装 core 再单独装 optional。

# ===== CORE (纯 Python / 有预编译 wheel) =====

# HTTP 服务
fastapi==0.115.6
uvicorn==0.34.0

# 向量嵌入 (sentence-transformers 自带 torch，有 wheel)
sentence-transformers==3.4.1

# 稀疏检索（纯 Python）
rank-bm25==0.2.2

# 统计嵌入后备方案（scikit-learn 有 wheel）
scikit-learn==1.6.1

# 数据验证（纯 Python）
pydantic==2.10.4

# 向量器持久化（TF-IDF 词表落盘，查询端复用；纯 Python）
joblib==1.5.3

# HTTP 客户端（纯 Python）
httpx==0.28.1

# 测试（纯 Python）
pytest==8.3.4
pytest-asyncio==0.25.0

# ===== OPTIONAL (需要编译 / 特定平台 wheel) =====

# 数值计算 — 如果下面这行报错，先注释它，
# 手动装: pip install numpy
#numpy

# 稠密向量检索 — 如果下面这行报错，先注释它，
# 手动装: pip install faiss-cpu
#faiss-cpu

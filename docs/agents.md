# Agent 体系

> 系统有 5 个专业 Agent，由总编 Agent 统一协调。每个 Agent 有明确的职责边界。

---

## 总编 Agent（大管家）

**职责**：理解需求、分派任务、协调流程、质量裁定

| 你说 | 总编做什么 |
|------|-----------|
| "帮我审稿" | 派审稿 Agent |
| "没灵感" | 派规划 Agent |
| "帮我写一章" | 先派上下文 Agent 打包 → 再派写作 Agent |
| "改编成短剧/出分镜" | 派短剧编剧 Agent（确认源文范围和规格后） |
| "一条龙" | 规划 → 上下文 → 写作 → 审稿，全流程协调 |

**不做**：不直接审查、不直接写、不自己构思、不改编短剧。

---

## 上下文 Agent（记忆中枢）

**职责**：主角状态追踪、伏笔管理、事件索引、上下文打包

### 写作前

打包当前状态 → 注入写作 Agent。上下文包包含：

```
━━ 上下文包 ━━
── 1. 当前任务 ──    ← 本章要写什么
── 2. 主角状态 ──    ← 主角精确的当前状态（身体/能力/处境/位置/目标）
── 3. 出场角色 ──    ← 本章涉及的角色
── 4. 活跃伏笔 ──    ← 未回收的伏笔（超期标记）
── 5. 风格参考 ──    ← 最近3章节奏特征
── 6. 约束提醒 ⚠    ← 不能违反的硬约束
```

### 写作后

- 更新主角状态
- 更新伏笔登记表
- 更新事件索引
- 生成章节摘要
- Git commit

### 维护的文件

| 文件 | 内容 | 更新时机 | 读取者 |
|------|------|---------|--------|
| `状态/主角.md` | 身体/能力/处境/位置/目标/关键资源 | 每章完成后 | 写作/规划/审稿 Agent |
| `状态/角色/{角色名}.md` | 配角状态/关系/已知信息 | 该角色出场后 | 写作/审稿 Agent |
| `状态/伏笔登记表.md` | 编号/内容/埋下章/预计回收章 | 每章完成后 | 规划 Agent |
| `记忆/事件索引.md` | 重大事件按章索引 | 每章完成后 | 快速定位历史事件 |
| `记忆/章节摘要/第N章.md` | 200 字章节概要 | 每章完成后 | 上下文包素材 |
| `记忆/风格参考.md` | 最近 5 章写作特征 | 每章完成后 | 风格一致性保持 |

---

## 规划 Agent（构思者）

**职责**：剧情构思、大纲生成、设定扩展、反转设计

- **输入**：当前故事状态 + 未回收伏笔
- **输出**：3-5 个剧情方向 + 对比表
- **原则**：不出单一方案，至少给 3 个选择

典型场景：

```
用户："没灵感了，后面怎么写？"
规划 Agent：
  → 读取当前主角状态、未回收伏笔
  → 分析剧情张力点
  → 输出 3 个可选方向（含优缺点对比）
  → 用户选择后深化为章节大纲
```

---

## 写作 Agent（写手）

**职责**：按大纲和上下文包生成正文

- **输入**：上下文包 + 章节大纲
- **输出**：2000-3000 字正文
- **约束**：遵守项目模板 + human-linguistics 语感规则
- **原则**：不自己编剧情、不审查自己的稿子

### 知识支持

写作 Agent 可通过 RAG 层查询相关知识：

| 查询 | 获取的知识 |
|------|-----------|
| "这段太像 AI 写的" | 去AI味最小修改指南 |
| "写之前该准备什么" | 章节写前准备清单 |
| "这个大纲能展开吗" | 大纲质量评估清单 |

---

## 短剧编剧 Agent（改编者）

**职责**：把已有小说（正文/大纲）改编为 AI 短剧，产出人物提示词与 10s 分镜脚本

- **输入**：源文范围（黄金三章 / 章末钩子 / 卷末高潮）+ 规格参数（单镜头时长 / 目标集数）
- **输出**：人物提示词（中英双语）+ 分镜脚本（画面 / 音效 / 台词字幕 / AI 提示词）
- **约束**：忠于原作核心设定，每集一个核心事件，三大留存钩子（前 10s / 第 15s / 片尾）齐全
- **原则**：不写正文、不规划剧情、不审查稿件，只做「小说 → 短剧」改编

### 适配流程

```
用户说"改编成短剧/出分镜"
  │
  ▼
总编确认源文范围 + 单镜头时长 + 目标集数
  │
  ▼
短剧编剧 Agent
  ├─ 读取正文/大纲，判断改编优先级
  ├─ 生成人物提示词（中英双语）
  ├─ 按 10s/镜 输出分镜脚本
  └─ 自检钩子节奏
  │
  ▼
总编核验三大钩子 → 呈现分镜文档
```

---

## 审稿 Agent（质检员）

**职责**：调用各审查子模块，产出统一审查报告

- **审查范围**：语感 / 设定 / 情节 / 节奏 / 题材规则
- **调用子模块**：game-datafied / human-linguistics / plot-review / rhythm-review
- **原则**：只挑毛病，不写替代方案

### 审查流程

```
审稿 Agent 收到正文
  │
  ├── human-linguistics  → 语感检查（总结腔、AI味、句式单一）
  ├── plot-review        → 情节检查（逻辑漏洞、伏笔回收）
  ├── rhythm-review      → 节奏检查（爽点密度、疲劳点）
  └── game-datafied      → 题材检查（仅游戏/数据化题材加载）
  │
  ▼
统一审查报告（分类列出问题，不替作者修改）
```

---

## Agent 协作流程

### 单章完整流水线

```
用户确认"写第N章"
  │
  ▼
① 上下文 Agent — 打包当前上下文
   ├─ 主角状态（身体/能力/处境/位置/目标）
   ├─ 出场角色
   ├─ 活跃伏笔（标记超期）
   ├─ 风格参考（最近3章特征）
   └─ 约束提醒（⚠ 不能违反的规则）
  │
  ▼
② 写作 Agent — 生成正文
   ├─ 接收上下文包 + 大纲
   ├─ 写 2000-3000 字
   ├─ RAG 查询：语感规则、写前清单
   └─ 遵守 human-linguistics 语感规则
  │
  ▼
③ 上下文 Agent — 写作后归档
   ├─ 更新主角状态
   ├─ 更新伏笔登记表
   ├─ 更新事件索引
   ├─ 生成章节摘要
   └─ git commit
  │
  ▼
④ 审稿 Agent — 质量检查
   ├─ 语感检查（human-linguistics）
   ├─ 情节检查（plot-review）
   ├─ 节奏检查（rhythm-review）
   └─ 题材检查（game-datafied）
  │
  ▼
⑤ 总编 Agent — 汇总呈现
   ├─ 展示修改建议
   └─ 等待用户确认
```
